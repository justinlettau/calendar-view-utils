/**
 * Calendar options.
 */
export interface CalendarOptions {
  weekStartsOn?: DayOfWeek;
}

/**
 * Day of week.
 */
export enum DayOfWeek {
  Sunday,
  Monday,
  Tuesday,
  Wednesday,
  Thursday,
  Friday,
  Saturday,
}
