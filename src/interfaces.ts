/**
 * Calendar options.
 */
export interface CalendarOptions {
  weekStartsOn?: DayOfWeek;
}

/**
 * Day of week.
 */
export enum DayOfWeek {
  Sunday,
  Monday,
  Tuesday,
  Wednesday,
  Thursday,
  Friday,
  Saturday,
}

/**
 * Month.
 */
export enum Month {
  January,
  February,
  March,
  April,
  May,
  June,
  July,
  August,
  September,
  October,
  November,
  December,
}
